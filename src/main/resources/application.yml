server:
  port: 8080 # ?? ?? ????

spring:
  application:
    name: Lucky7-PostService

  # H2 Database ??
  datasource:
    driver-class-name: org.h2.Driver
    url: ${DATABASE_URL}    # H2 DB ?? ?? (Embedded Mode)
    username: ${DATABASE_USER_NAME}        # H2 DB ?? ID (??? ??)
    password: ${DATABASE_PASSWORD}        # H2 DB ?? PW (??? ??)

  # H2 Console ??
  h2:
    console: # H2 DB? ??? ??? ? ?? ??
      enabled: true           # H2 Console ?? ??
      path: /h2-console       # H2 Console ?? ??

  # JPA ??
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update        # DB ??? ?? (none, create, create-drop, update, validate)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true      # ?? ?? ?? (??)
        show_sql: true        # ?? ?? ??